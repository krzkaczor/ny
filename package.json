{
  "name": "ny",
  "private": true,
  "version": "0.1.1",
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  },
  "scripts": {
    "version": "changeset version && bun .changeset/sync-cargo-version.ts",
    "publish": "git tag v`cat package.json | jq -r .version`"
  }
}
